{
  "scripts": {
    "test": "jest",
    "prebuild": "del dist",
    "build": "babel src --extensions .ts --out-dir dist --ignore 'src/**/*.test.js'",
    "build:type": "tsc --emitDeclarationOnly",
    "prepare": "yarn build && yarn build:type",
    "typecheck": "tsc --noEmit"
  }
}
